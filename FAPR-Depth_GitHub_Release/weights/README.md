# Checkpoints

Checkpoints are not included in this archive.

Expected optional files:

```text
weights/
├── fapr_v5_best_score.pth
└── best_candidate.pth
```

For GitHub releases, upload large weights as Release assets or track them with
Git LFS. Publish checksums and clearly state which output is used for the main
benchmark.
